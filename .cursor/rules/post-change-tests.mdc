---
description: ONLY Apply this rule when code changes have been made to the python code.
alwaysApply: false
---
After each Python code change, run in order: `pipenv run flake8` -> `pipenv run black .` -> `pipenv run pytest --ignore=tests/test_cli_e2e.py`; ensure tests pass before proceeding.
